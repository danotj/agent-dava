services:
  agent:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      PORT: 8000
    volumes:
      - ./knowledge:/app/knowledge
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
